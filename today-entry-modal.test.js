const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs");
const html=fs.readFileSync("index.html","utf8"),app=fs.readFileSync("app.js","utf8"),css=fs.readFileSync("style.css","utf8");
test("Today Summary card opens the accessible entry dialog for the displayed date",()=>{assert.match(html,/id="todaySummaryCard"[^>]+role="button"[^>]+aria-controls="todayEntryModal"/);assert.match(app,/openTodayEntry\(summaryTargetDate\(\)\)/);assert.match(app,/event\.key==="Enter"\|\|event\.key===" "/)});
test("entry modal unifies business and clinical fields",()=>{const modal=html.match(/<div class="today-entry-modal"[\s\S]*?<\/form>/)[0];for(const id of ["todayEntrySales","todayEntryPatients","todayEntryNewPatients","todayEntryNewPatientRate","todayEntryMemo"])assert.match(modal,new RegExp(`id="${id}"`));for(const metric of ["preventive","checkups","imaging","bloodTests","surgeries","trimming","secondOpinions"])assert.match(modal,new RegExp(`data-modal-clinical-output="${metric}"`));assert.doesNotMatch(modal,/todayEntry(?:Revisits|ProfitRate)/)});
test("modal restores and rewrites one compatible entry",()=>{assert.match(app,/data\.entries\.find\(item=>item\.date===date\)/);assert.match(app,/entry=\{\.\.\.existing,date,clinicalTags:/);assert.match(app,/applyTodayEntryClinical\(entry\)/);assert.match(app,/if\(index>=0\)data\.entries\[index\]=entry/);assert.match(app,/render\(\);toast/)});
test("sheet supports backdrop close and mobile sizing",()=>{assert.match(app,/if\(event\.target===modal\)closeTodayEntry/);assert.match(css,/width:min\(95vw,620px\)/);assert.match(css,/border-radius:24px 24px 0 0/);assert.match(css,/translateY\(100%\)/)});
