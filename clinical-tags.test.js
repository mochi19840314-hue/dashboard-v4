const test=require("node:test"),assert=require("node:assert/strict"),Tags=require("./clinical-tags");
test("missing clinicalTags is normalized as an empty list",()=>assert.deepEqual(Tags.normalize(undefined),[]));
test("analysis waits for three tagged days and does not assert causality",()=>{const e=(date,tag,sales)=>({date,patients:10,sales,clinicalTags:tag?["exam_proposal_low"]:[],clinical:{xrays:tag?0:3}});assert.equal(Tags.analyze([e("2026-08-01",1,8e4),e("2026-08-02",1,8e4),e("2026-08-03",0,12e4)]).length,0);const result=Tags.analyze([e("2026-08-01",1,8e4),e("2026-08-02",1,8e4),e("2026-08-03",1,8e4),e("2026-08-04",0,12e4)]);assert.equal(result[0].confidence,"参考傾向");assert.match(Tags.sentence(result[0]),/傾向があります/);assert.doesNotMatch(Tags.sentence(result[0]),/ため|原因|因果/)});
