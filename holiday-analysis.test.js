const test=require("node:test"),assert=require("node:assert/strict"),HolidayAnalysis=require("./holiday-analysis");
const entry=(date,sales,patients,extra={})=>({date,sales,patients,...extra});
test("8/10〜8/16は営業日だけを集計し前後28日と比較する",()=>{const rows=[entry("2026-08-10",999,99),entry("2026-08-11",100,10),entry("2026-08-12",200,10),...Array.from({length:5},(_,i)=>entry(`2026-08-0${i+3}`,300,15))],r=HolidayAnalysis.analyze(rows,{start:"2026-08-10",end:"2026-08-16",isClosed:d=>d==="2026-08-10"});assert.equal(r.target.days,2);assert.equal(r.target.averageSales,150);assert.equal(r.target.averagePatients,10);assert.equal(r.target.unitPrice,15);assert.equal(r.normal.averageSales,300);assert.equal(r.changes.sales,-50);assert.equal(r.value,"low")});
test("未入力日を除外し比較不足でもNaNを返さない",()=>{const r=HolidayAnalysis.analyze([{date:"2026-08-11",sales:"",patients:""},entry("2026-08-12",0,0)],{start:"2026-08-10",end:"2026-08-16"});assert.equal(r.target.days,1);assert.equal(r.sufficient,false);assert.equal(r.changes.sales,null);assert.equal(Number.isNaN(r.changes.sales),false)});
test("お盆プリセットは8月10日〜16日",()=>assert.deepEqual(HolidayAnalysis.preset("obon",2026),{start:"2026-08-10",end:"2026-08-16",label:"お盆"}));
