const test=require("node:test"),assert=require("node:assert/strict"),Brief=require("./morning-executive-brief");
test("通常日は既存AIソースを最大5項目に統合する",()=>{const r=Brief.build({date:"2026-08-18",hour:8,health:{score:89,grade:{label:"Good"}},healthHistory:[{date:"2026-08-17",score:87}],optimizer:{candidates:[{label:"画像検査",expectedProfit:18000,successRate:91,reason:"82営業日の相関"}]},strategyMap:{priorities:[{theme:"健診",reason:"減少傾向"}]},learning:{theme:"火曜日の画像検査"},target:5000000,sales:4100000});assert.equal(r.items.length,5);assert.equal(r.priority.label,"画像検査");assert.equal(r.previousDelta,2);assert.equal(r.goal.achievement,82);assert.equal(r.learning.theme,"火曜日の画像検査")});
test("休診日は経営提案に切り替える",()=>{const r=Brief.build({date:"2026-08-18",closed:true,optimizer:{candidates:[{label:"画像検査"}]}});assert.equal(r.mode,"closed");assert.equal(r.priority.label,"経営メンテナンス");assert.match(r.priority.reason,/LINE配信/)});
test("データ不足とJSON互換を安全に扱う",()=>{const r=Brief.build({date:"2026-08-18"});assert.equal(r.score,null);assert.equal(Brief.normalizeHistory(null).length,0);assert.deepEqual(Brief.normalizeBrief({items:[1,2,3,4,5,6]}).items,[1,2,3,4,5])});
test("営業終了後はAI会議レビューになる",()=>assert.equal(Brief.build({date:"2026-08-18",hour:18}).mode,"review"));
