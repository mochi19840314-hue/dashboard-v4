const test=require("node:test"),assert=require("node:assert/strict"),CashFlowForecast=require("./cash-flow-forecast.js");
test("月末残高と前月比を計算する",()=>{const result=CashFlowForecast.calculate({balance:18250000,currentSales:5000000,projectedSales:9180000,payments:{salary:2000000,rent:500000,medical:500000,card:20000,lease:200000,tax:200000,other:100000},previousBalance:17710000,dayOfMonth:26});assert.equal(result.incoming,4180000);assert.equal(result.outgoing,3520000);assert.equal(result.forecastBalance,18910000);assert.equal(result.monthOverMonth,1200000);assert.equal(result.safety.stars,"★★★★☆")});
test("安全度の境界値と色を返す",()=>{assert.deepEqual(CashFlowForecast.safety(2999999),{level:1,stars:"★☆☆☆☆",tone:"red"});assert.equal(CashFlowForecast.safety(3000000).tone,"orange");assert.equal(CashFlowForecast.safety(5000000).tone,"yellow");assert.equal(CashFlowForecast.safety(10000000).tone,"blue");assert.equal(CashFlowForecast.safety(20000000).tone,"green")});
test("未入力値を0として扱い残高不足を通知する",()=>{const result=CashFlowForecast.calculate({balance:1000000,payments:{tax:2000000}});assert.equal(result.forecastBalance,-1000000);assert.match(result.comment,/安全圏/) });
