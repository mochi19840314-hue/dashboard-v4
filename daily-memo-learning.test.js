"use strict";
const test=require("node:test"),assert=require("node:assert/strict"),Memo=require("./daily-memo-learning"),Backup=require("./backup-normalizer");
test("メモはトリムし、改行・絵文字を保持して500文字に制限する",()=>{const value=`  皮膚科 🐈\r\n${"あ".repeat(600)}  `,clean=Memo.clean(value);assert.equal(clean.includes("🐈\n"),true);assert.equal([...clean].length<=501,true);assert.equal(clean.length,500)});
test("旧JSONのnoteをentry.memoへ補完し未入力は空文字にする",()=>{const defaults={entries:[],finance:{},clinic:{closedDates:[]}};const result=Backup.normalizeBackup({entries:[{date:"2026-08-17",note:" 旧メモ "},{date:"2026-08-18"}]},defaults,"key").data;assert.equal(result.entries[0].memo," 旧メモ ");assert.equal(result.entries[1].memo,"")});
test("18時以降だけ学習しKnowledge Core用TOP10と傾向を作る",()=>{const entries=Array.from({length:10},(_,i)=>({date:`2026-08-${String(i+1).padStart(2,"0")}`,memo:i>4?"紹介と皮膚科、午後キャンセル":"手術"}));assert.equal(Memo.learnAtNight({entries,today:"2026-08-18",hour:17}).saved,false);const learned=Memo.learnAtNight({entries,today:"2026-08-18",hour:18});assert.equal(learned.saved,true);assert.ok(learned.knowledge.keywords.length<=10);assert.ok(learned.record.categories.includes("季節性"));assert.ok(learned.trends.length<=3)});
