const test=require("node:test"),assert=require("node:assert/strict"),DailyReview=require("./daily-review");
const entry=(date,sales=180000,patients=15,extra={})=>({date,sales,patients,newPatients:2,surgeries:1,checkups:2,clinical:{bloodTests:3,xrays:2,ultrasounds:1,preventive:2},...extra});
test("3日未満の比較データでは蓄積中にする",()=>assert.deepEqual(DailyReview.build({date:"2026-08-14",entry:entry("2026-08-14"),entries:[entry("2026-08-12"),entry("2026-08-13")]}),{ready:false}));
test("採点から明日の行動を1件だけ返す",()=>{const entries=[entry("2026-08-10"),entry("2026-08-11"),entry("2026-08-12"),entry("2026-08-13")],today=entry("2026-08-14",150000,15,{newPatients:0,clinical:{bloodTests:1,xrays:0,ultrasounds:0,preventive:1}}),result=DailyReview.build({date:today.date,entry:today,entries,profitRate:28});assert.equal(result.ready,true);assert.equal(typeof result.priority,"string");assert.ok(!result.priority.includes("、"));assert.ok(result.effect.value>0);assert.ok(result.comment.length<60);assert.ok(result.score>=0&&result.score<=100)});
