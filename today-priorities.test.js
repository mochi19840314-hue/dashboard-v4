const test=require("node:test"),assert=require("node:assert/strict"),TodayPriorities=require("./today-priorities.js");
const entry=(date,sales,patients,newPatients=2,surgeries=1)=>({date,sales,patients,newPatients,surgeries,clinical:{bloodTests:3,xrays:2,ultrasounds:1}});
test("three days are required before priorities are proposed",()=>{assert.equal(TodayPriorities.build({entries:[entry("2026-08-01",100000,10),entry("2026-08-02",100000,10)]},{today:"2026-08-14"}).ready,false)});
test("priorities are ranked by impact, gap and urgency and limited to three",()=>{const entries=[entry("2026-07-01",200000,20,5,3),entry("2026-07-02",220000,20,5,3),entry("2026-07-03",210000,20,5,3),entry("2026-08-01",80000,10,0,0),entry("2026-08-02",70000,9,0,0),entry("2026-08-03",60000,8,0,0)];const result=TodayPriorities.build({entries,financeByMonth:{"2026-08":{monthlyExpense:190000,medicalExpense:100000}}},{today:"2026-08-14",anomalies:[{metric:"来院件数",level:"danger"}]});assert.equal(result.ready,true);assert.ok(result.items.length<=3);assert.equal(result.items[0].key,"patients");assert.ok(result.items.every(item=>item.importance>=3&&item.importance<=5));assert.ok(result.items.every((item,index,array)=>!index||array[index-1].score>=item.score))});
